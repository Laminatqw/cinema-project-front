import React from 'react';
import LoginationComponent from "../../components/LoginationComponent/LoginationComponent";

const LoginPage = () => {
    return (
        <div>
            <LoginationComponent/>
        </div>
    );
};

export default LoginPage;